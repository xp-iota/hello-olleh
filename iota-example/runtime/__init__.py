"""Shared runtime package for the offline iota teaching modules."""

import logging

# Optional kernel integrations may be absent in the offline teaching environment.
logging.disable(logging.CRITICAL)
