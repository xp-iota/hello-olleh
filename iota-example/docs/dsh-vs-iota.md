# DSH vs iota：M01–M12 逐模块证据表

## 读表规则

- **A**：iota-core 已有语义等价能力；**B**：只消费已完成的 ToolPipeline；**C**：结构性差异，不为形状对齐扩大 core。
- `iota-core/...` 指本工程 `pyproject.toml` 的本地 editable source；本次核对基线为 `e3eb098d041f0cb9620e1840aa2d1852f417d75c`。
- 每行的 DSH 结论、iota 结论和可运行证明分别给出 `文件:行`。类 C 还显式列出既有决策记录；M04/M05 的 D7 范围裁决见 `dsh-workshop/PLAN.md:340`。

## 对照表

| 模块 | DSH 事实 | iota 事实与分类 | 本工程可运行证明 | 类 C 既有决策依据 |
|---|---|---|---|---|
| **M01 工具管线** | DSH 分开注册、pre-execute、post-execute、restrict、guard 五阶段（`dsh-example/M01-tool-pipeline/README.md:14-20`）。 | **B + C**：真实 `ToolPipeline` 提供具名可逆 stage、重复安装跳过和缺失报告（`iota-core/src/iota_core/tool_pipeline.py:145-176`、`iota-core/src/iota_core/tool_pipeline.py:199-265`）；iota 图节点明确不承诺 per-node 工具 allowlist（`iota-core/src/iota_core/graph/types.py:276-279`、`iota-core/src/iota_core/graph/types.py:313-327`）。 | 两层结果为 `audit(guard(raw:x))`，错名 stage 抛 `UnknownStageError`（`iota-example/M01-tool-pipeline/index.py:45-64`）。 | 模型/工具执行栈归内核，iota 只能承诺自己的边界（`iota-core/docs/architecture/node-hooks.md:12-13`）。 |
| **M02 上下文装配与经济学** | DSH 有 section、waterfall assembly 与 CompactionEngine（`dsh-example/M02-context-assembly-economics/README.md:13-17`）。 | **A + C**：`MemoryContextService` 用 user/project/session scope 调 MemoryGateway（`iota-core/src/iota_core/memory/context.py:39-69`），且记忆 prompt 默认不注入（`iota-core/src/iota_core/types.py:341-359`）；模型请求装配仍是内核执行栈。 | 构建 scoped prefix，验证 `included_count=1`、session scope 与默认关闭（`iota-example/M02-context-assembly-economics/index.py:42-62`）。 | iota 无法替内核内部模型调用承诺装配/压缩 seam（`iota-core/docs/architecture/node-hooks.md:9-13`）。 |
| **M03 推理服务接入** | DSH 替换 `LlmAdapter.stream()` 并以 `llm/stream` waterfall 包流（`dsh-example/M03-inference-service-access/README.md:14-19`）。 | **C**：iota 可换单元是具备 start/capabilities/create_session/stream/close 的整个 `KernelAdapter`（`iota-core/src/iota_core/adapters/base.py:155-170`、`iota-core/src/iota_core/adapters/base.py:172-177`、`iota-core/src/iota_core/adapters/base.py:270-273`）；`register_adapter` 注册这个单元（`iota-core/src/iota_core/providers.py:76-101`）。 | 用真实 `HermesAcpAdapter.capabilities()` 驱动编译，`llm_execution` 被拒绝（`iota-example/M03-inference-service-access/index.py:18-48`）。 | ACP 对模型 middleware 编译期拒绝且无静默降级（`iota-core/docs/architecture/node-hooks.md:67-76`、`iota-core/docs/architecture/node-hooks.md:121-133`）。 |
| **M04 Agent 循环与干预面** | DSH 暴露遥测、steering 与 inbox 的 next-turn/next-step 边界（`dsh-example/M04-agent-loop-intervention/README.md:13-19`）。 | **A + C**：iota 有判别联合 `AgentEvent`（`iota-core/src/iota_core/types.py:394-501`）和对所有 executor/kernel 生效的结果/错误 hook（`iota-core/docs/architecture/node-hooks.md:5-13`）；D7 未补 mid-turn inbox。 | 真正 stream 出 system_init/text_delta/final，并执行 `after_node_result`（`iota-example/M04-agent-loop-intervention/index.py:10-39`）。 | 中途注入会越过 orchestrator 所有的节点边界进入内核执行栈（`iota-core/docs/architecture/node-hooks.md:12-13`）；范围裁决（`dsh-workshop/PLAN.md:340`）。 |
| **M05 会话面** | DSH 以追加日志为事实、surface 为投影，并承诺连续 seq/深冻结/replace/fork（`dsh-example/M05-session-surface/README.md:13-19`）。 | **A + C**：`ConversationStore` 承诺有序消息（`iota-core/src/iota_core/storage/conversation_store.py:15-29`），`RunStore` 记录 run 状态和有序事件（`iota-core/src/iota_core/storage/run_store.py:20-46`）；现有事件 schema 没有 DSH 的 seq/surface 契约（`iota-core/src/iota_core/types.py:394-501`）。 | 实际读取两条消息、succeeded run 与三事件，并验证无 `sequence`（`iota-example/M05-session-surface/index.py:8-27`）。 | 无 Definition/Provider/Consumer 三角色不构成新 seam（`iota-core/docs/architecture/pluggability-cordis-alignment.md:201-203`）；范围裁决（`dsh-workshop/PLAN.md:340`）。 |
| **M06 人在环路** | DSH 把 slash command 与 approval 做成两个显式协议，另有 planMode/todo/feedback（`dsh-example/M06-human-in-the-loop/README.md:13-20`、`dsh-example/M06-human-in-the-loop/README.md:23-30`）。 | **A + C**：ACP 基类处理 `session/request_permission`，无匹配策略时 fail closed（`iota-core/src/iota_core/adapters/acp_stdio.py:694-717`），并按 option kind 选择（`iota-core/src/iota_core/adapters/acp_stdio.py:719-743`）；planMode 不属于 orchestrator schema。 | 直接驱动真实 DSH ACP adapter，默认选择 reject option（`iota-example/M06-human-in-the-loop/index.py:11-34`）。 | 未答复 permission 会挂死整轮，默认必须拒绝（`iota-core/docs/architecture/kernel-replacement.md:125-136`）；自建完整 harness 被否决（`iota-core/docs/architecture/kernel-replacement.md:249-255`）。 |
| **M07 执行侧后端** | DSH 的 fs/shell/sandbox seam 把 I/O、后端与约束分层（`dsh-example/M07-execution-backends/README.md:13-21`）。 | **C**：iota 的 `KernelAdapter.stream()` 只接收 prompt/memory 并转发内核事件（`iota-core/src/iota_core/adapters/base.py:172-177`），不提供 ctx.fs/ctx.shell/sandbox；工具事件只是内核返回的数据（`iota-core/src/iota_core/types.py:404-415`）。 | shell 请求产生 `kernel.shell` start/result，同时 orchestrator 工具 registry 为空（`iota-example/M07-execution-backends/index.py:10-23`）。 | 在 cordis 上补执行栈等于重写 DSH harness，不建议（`iota-core/docs/architecture/kernel-replacement.md:245-258`）。 |
| **M08 委派与预设** | DSH 注册 SubagentProvider，再由显式工具调用 `ctx.subagents.start()`（`dsh-example/M08-delegation-presets/README.md:11-18`）。 | **A**：iota 的 `NodeSpec.depends_on`、input bindings 与 `GraphSpec` DAG 是语义等价委派形状（`iota-core/src/iota_core/graph/types.py:266-309`、`iota-core/src/iota_core/graph/types.py:410-424`）。 | 编译并执行 research→review，拓扑与绑定结果均被断言（`iota-example/M08-delegation-presets/index.py:12-50`）。 | 不适用。 |
| **M09 长任务与编排** | DSH 将 Job 运行句柄与 Goal 状态机分开（`dsh-example/M09-long-running-orchestration/README.md:11-20`）。 | **A（反向）**：iota `GraphTask` 自带 task-id identity、lease、attempt 与状态（`iota-core/src/iota_core/graph/task_queue.py:23-54`），queue 支持 enqueue/claim/heartbeat/ack/fail/DLQ（`iota-core/src/iota_core/graph/task_queue.py:57-75`），checkpoint 冻结节点状态与 sequence（`iota-core/src/iota_core/graph/checkpoint.py:23-36`）。 | 跑出幂等 enqueue、claim/ack succeeded 与 checkpoint sequence=1（`iota-example/M09-long-running-orchestration/index.py:12-49`）。 | 不适用。 |
| **M10 外部能力接入** | DSH 以 `SKILL.md` 提供可发现、可注入知识，并把 MCP 放在外部运行面（`dsh-example/M10-external-capabilities/README.md:12-21`、`dsh-example/M10-external-capabilities/README.md:24-28`）。 | **A**：`sync_skills` 发现并投影 SKILL.md（`iota-core/src/iota_core/skill_sync.py:107-153`）；`IotaMcpServer` 注册工具并分发 tools/list、tools/call（`iota-core/src/iota_core/mcp/server.py:54-87`、`iota-core/src/iota_core/mcp/server.py:110-150`）。 | 临时目录同步 Skill，内存完成 MCP list/call（`iota-example/M10-external-capabilities/index.py:14-54`）。 | 不适用。 |
| **M11 配置与数据设施** | DSH settings 有 namespace/schema/revision，另有 storage/attachments/credentials/workspace 服务链（`dsh-example/M11-config-data-infrastructure/README.md:11-15`、`dsh-example/M11-config-data-infrastructure/README.md:18-28`）。 | **A + C**：iota profile 由 adapter projector 解析（`iota-core/src/iota_core/config_profiles/manager.py:29-74`），ConversationStore 是可换协议（`iota-core/src/iota_core/storage/conversation_store.py:15-52`）；dsh 无 iota-managed layout 时明确拒绝（`iota-core/src/iota_core/providers.py:152-171`），凭证/附件不塞入 AgentConfig（`iota-core/src/iota_core/types.py:341-364`）。 | 真投影 Claude profile、写内存 store、跑出 dsh profile fail-loud（`iota-example/M11-config-data-infrastructure/index.py:17-53`）。 | dsh 配置通过命令行读取，硬造 profile 会生成内核不读的目录（`iota-core/docs/architecture/kernel-replacement.md:157-159`）。 |
| **M12 框架机制本体** | DSH/Cordis 提供 dispatch/Fiber，另有 pending、isolate、intercept（`dsh-example/M12-framework-mechanisms/README.md:11-20`、`dsh-example/M12-framework-mechanisms/README.md:23-27`）。 | **A + C**：`EffectStack` 是 LIFO、幂等且失效后拒绝（`iota-core/src/iota_core/effects.py:52-65`、`iota-core/src/iota_core/effects.py:86-105`、`iota-core/src/iota_core/effects.py:122-133`）；registry disposer 做身份比对（`iota-core/src/iota_core/registry.py:33-60`）；capability 错名类定义期拒绝（`iota-core/src/iota_core/capabilities.py:12-23`）；外部 provider 走 entry-point（`iota-core/src/iota_core/providers.py:178-206`）。 | 跑出 LIFO/幂等/inactive 拒绝/身份 disposer/capability 拒绝/entry-point 名（`iota-example/M12-framework-mechanisms/index.py:15-66`）。 | 借机制不搬 Proxy/Fiber 架构（`iota-core/docs/architecture/pluggability-cordis-alignment.md:182-199`）；明确不做 HMR、Proxy Context、intercept、自建 harness（`iota-core/docs/architecture/pluggability-cordis-alignment.md:312-320`）。 |

## 可复现命令

```bash
env -u PYTHONHOME -u PYTHONPATH .venv/bin/python -m runtime.run_all
env -u PYTHONHOME -u PYTHONPATH .venv/bin/pytest -q
env -u PYTHONHOME -u PYTHONPATH .venv/bin/ruff check .
env -u PYTHONHOME -u PYTHONPATH .venv/bin/mypy
```
