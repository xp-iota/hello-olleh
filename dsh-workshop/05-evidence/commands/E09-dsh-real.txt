$ cd dsh-example && npm run M09:real


> @example/dsh-examples@0.0.0 M09:real
> DSH_REAL=1 node M09-long-running-orchestration/run-real.ts


████ M09 · 长任务与编排：运行句柄与目标状态 —— 真实 MiniMax 模式 ████
provider=minimax-m3 model=MiniMax-M3 endpoint=<minimax-anthropic-compat> timeout=120000ms

════════ M09.1 · 管理后台 Job 的启动、读取、终止与等待（kind=mechanism）════════
  ↳ 入口 probe 真实回复: "装配链已连通 M09.1"
① 启动一个长跑任务（JobHooks：run() 同步返回 { cancel, done, readOutput }）:
   颁发 id = bash-1 （<kind>-N 形）

② read 是增量读（连续读不重复；游标归生产者）:
   第一次读: "进度 1/3\n"
   第二次读: "" （只剩增量）

③ wait：等结算或超时（不取消任务）:
  [jobs] 完成通告：bash-1 → completed (exit code: 0) (owner=demo-session)
   结算状态 = completed | detail = exit code: 0 | reported = true

④ 属主栅栏：非属主读取被拒:
   job bash-1 belongs to another session

⑤ kill：live → stopping → killed（reason 原样转发给 hooks.cancel）:
   kill → requested
  [jobs] 完成通告：bash-2 → killed (演示取消)
   结算 = killed

⑥ 准入：没有 controller 时 start 被拒（先卸掉插件的 controller）:
   background jobs unavailable: no job controller serves this agent (load @deepseek-ai/dsh-tool-jobs in its composition)
REAL_STAGE_OK M09.1 kind=mechanism provider=minimax-m3 model=MiniMax-M3 calls=1 ms=1364 in=0 out=9 finish=stop tools=- text="装配链已连通 M09.1"

════════ M09.2 · 以追加日志和 CAS 推进 Goal 生命周期（kind=mechanism）════════
  ↳ 入口 probe 真实回复: "装配链已连通 M09.2"
① create：创建并 arm（rev=1, phase=active, activation=armed）
  [goal/change] seq=0 → rev=1 phase=active "重构支付模块"
   → active armed rev=1

② pause：active → paused（撤 arm）
  [goal/change] seq=1 → rev=2 phase=paused "重构支付模块"
   → paused disarmed rev=2

③ CAS 冲突：用旧 ref 操作被拒
   stale goal ref "goal-3d891a84-1eff-481e-8eed-84a14be0d262" revision 1; current is "goal-3d891a84-1eff-481e-8eed-84a14be0d262" revision 2

④ resume：paused → active（重新 arm）
  [goal/change] seq=2 → rev=3 phase=active "重构支付模块"
   → active armed rev=3

⑤ block：→ blocked（携带人机可读 reason）
  [goal/change] seq=3 → rev=4 phase=blocked "重构支付模块"
   → blocked (code=needs-human) rev=4

⑥ complete：→ complete 并撤 arm
  [goal/change] seq=4 → rev=5 phase=complete "重构支付模块"
   → complete disarmed rev=5

⑦ clear：追加 null 墓碑，折叠结果回到 undefined
   goals.get(agent) = undefined

⑧ 日志即状态：回放 6 条 goal/change 事件即可重建全程
REAL_STAGE_OK M09.2 kind=mechanism provider=minimax-m3 model=MiniMax-M3 calls=1 ms=867 in=0 out=9 finish=stop tools=- text="装配链已连通 M09.2"

════════ M09.3 · 在 worker thread 执行工作流脚本（kind=mechanism）════════
  ↳ 入口 probe 真实回复: "装配链已连通 M09.3"
workflowEngine: {
  provider: 'WorkerThreadWorkflowEngine',
  stopReason: 'completed',
  value: { ok: true, value: 42 },
  agentsStarted: 0
}
REAL_STAGE_OK M09.3 kind=mechanism provider=minimax-m3 model=MiniMax-M3 calls=1 ms=995 in=0 out=9 finish=stop tools=- text="装配链已连通 M09.3"

════════ M09.4 · 把计划调度注册到耐久会话（kind=mechanism）════════
  ↳ 入口 probe 真实回复: "装配链已连通 M09.4"
schedule: {
  persistence: 'JsonlSessionPersistence',
  tools: [ 'schedule_create', 'schedule_list', 'schedule_delete' ]
}
REAL_STAGE_OK M09.4 kind=mechanism provider=minimax-m3 model=MiniMax-M3 calls=1 ms=927 in=0 out=9 finish=stop tools=- text="装配链已连通 M09.4"

REAL_MODULE_OK M09 stages=4 calls=4 failed=0
[exit 0]
