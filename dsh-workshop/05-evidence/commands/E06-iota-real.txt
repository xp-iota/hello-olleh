$ cd iota-example && env -u PYTHONHOME -u PYTHONPATH .venv/bin/python -m runtime.runner M06 --real

[claude-code:unrecognized_model] {"model":"MiniMax-M3","query_source":"sdk"}

████ M06 · 人在环路 ████
学习目标：验证 ACP permission 必定得到答复，并识别计划模式的宿主边界。
运行时观察：
  1. 无匹配策略时是否 fail closed
  2. permission 是否及时答复
  3. plan mode 位于哪一层
真实内核证据：
  - 真实模型: MiniMax-M3
  - 真实内核: claude（claude-agent-sdk + claude）
  - 真实响应: ACP 权限由主机裁定，计划模式边界止于只读提案。
  - 事件序列: step_start, system_init, thinking, text_delta, final, step_end
三类边界：
  - MiniMax-backed kernel 行为: 内核在权限策略下作答；请求必定得到答复，不会挂住整轮。
  - iota 编排层本地机制断言 : ACP permission 的 fail-closed 默认与 AgentConfig 字段边界在本地断言。
  - 明确不属于 iota 的能力  : 计划模式由具体内核或宿主管理，不在编排层。
验证结果：
  - permission: "no"
  - hang_prevention: "request answered"
  - plan_mode: "kernel/harness-owned"
对照关系：语义等价 + 结构性边界
结论：权限请求由适配器可靠闭合；计划模式由具体内核或宿主管理。
{"hang_prevention": "request answered", "module": "M06", "permission": "no", "plan_mode": "kernel/harness-owned", "relationship": "语义等价 + 结构性边界", "status": "ok"}
IOTA_REAL_MODULE_OK M06-human-in-the-loop kernel=claude model=MiniMax-M3 chars=25 events=6
[exit 0]
