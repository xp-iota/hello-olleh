$ cd iota-example && env -u PYTHONHOME -u PYTHONPATH .venv/bin/python -m runtime.runner M07 --real

[claude-code:unrecognized_model] {"model":"MiniMax-M3","query_source":"sdk"}
[claude-code:unrecognized_model] {"model":"MiniMax-M3","query_source":"sdk"}

████ M07 · 执行侧后端 ████
学习目标：确认 shell 副作用由 KernelAdapter 执行，而非编排层工具注册表。
运行时观察：
  1. kernel.shell 事件
  2. 工具结果事件
  3. 编排层 shell registry 是否为空
真实内核证据：
  - 真实模型: MiniMax-M3
  - 真实内核: claude（claude-agent-sdk + claude）
  - 真实响应: Shell 副作用由 KernelAdapter 执行，编排层工具注册表仅做声明。
  - 事件序列: step_start, system_init, thinking, text_delta, final, step_end
三类边界：
  - MiniMax-backed kernel 行为: shell 副作用由内核执行并以工具事件回传，编排层只转发。
  - iota 编排层本地机制断言 : 编排层 shell 注册表为空这一条本身就是可运行的边界证据。
  - 明确不属于 iota 的能力  : fs/shell/sandbox 执行栈不属于 iota。
验证结果：
  - route: "prompt -> KernelAdapter -> kernel tool events"
  - kernel: "claude"
  - kernel_tool_calls: ["Bash"]
  - execution_proof: "token read by kernel"
  - orchestrator_shell_registry: []
对照关系：结构性边界
结论：iota 转发执行事件，但不重写内核的 fs/shell/sandbox 执行栈。
{"execution_proof": "token read by kernel", "kernel": "claude", "kernel_tool_calls": ["Bash"], "module": "M07", "orchestrator_shell_registry": [], "relationship": "结构性边界", "route": "prompt -> KernelAdapter -> kernel tool events", "status": "ok"}
IOTA_REAL_MODULE_OK M07-execution-backends kernel=claude model=MiniMax-M3 chars=41 events=6
[exit 0]
