$ cd iota-example && env -u PYTHONHOME -u PYTHONPATH .venv/bin/python -m runtime.run_all


████ M01 · 工具管线 ████
学习目标：理解 iota 自有工具槽如何获得具名、可逆的处理阶段。
运行时观察：
  1. stage 安装顺序与幂等行为
  2. 错名 stage 是否明确拒绝
  3. effect 回收后 handler 是否恢复
验证结果：
  - pipeline: "audit(guard(raw:x))"
  - refusal: "unknown stage 'typo' on pipeline 'iota-tools'; declared stages: audit, guard"
对照关系：教学补齐 + 结构性边界
结论：ToolPipeline 可治理 iota 自有工具槽，但不冒充内核内部工具可见性控制。
{"module": "M01", "pipeline": "audit(guard(raw:x))", "refusal": "unknown stage 'typo' on pipeline 'iota-tools'; declared stages: audit, guard", "relationship": "教学补齐 + 结构性边界", "status": "ok"}
IOTA_MODULE_OK M01-tool-pipeline

████ M02 · 上下文装配与经济学 ████
学习目标：观察作用域记忆如何进入 Prompt，并识别仍由内核拥有的压缩边界。
运行时观察：
  1. user/project/session scope
  2. 记忆注入的默认开关
  3. 编排层是否声明 compression seam
验证结果：
  - scope: "session-a"
  - default_injection: false
  - compression_seam: "kernel-owned"
对照关系：语义等价 + 结构性边界
结论：iota 能组织作用域记忆；模型请求的完整装配与压缩仍属于内核。
{"compression_seam": "kernel-owned", "default_injection": false, "module": "M02", "relationship": "语义等价 + 结构性边界", "scope": "session-a", "status": "ok"}
IOTA_MODULE_OK M02-context-assembly-economics

████ M03 · 推理服务接入 ████
学习目标：理解 iota 以整个 KernelAdapter 为替换单元的设计。
运行时观察：
  1. Provider 注册的替换粒度
  2. ACP capability 声明
  3. 不支持的模型 middleware 是否编译期拒绝
验证结果：
  - replaceable_unit: "KernelAdapter"
  - acp_refusal: "node 'infer': kernel 'acp' cannot activate per-node model middleware/hooks ['llm_execution']; remove them or use a kernel that advertises per_session_middleware"
对照关系：结构性边界
结论：iota 替换整个内核适配器，不在编排层仿造 DSH 的 LLM middleware。
{"acp_refusal": "node 'infer': kernel 'acp' cannot activate per-node model middleware/hooks ['llm_execution']; remove them or use a kernel that advertises per_session_middleware", "module": "M03", "relationship": "结构性边界", "replaceable_unit": "KernelAdapter", "status": "ok"}
IOTA_MODULE_OK M03-inference-service-access

████ M04 · Agent 循环与干预面 ████
学习目标：区分标准事件流、节点 hook 与内核运行中的消息注入。
运行时观察：
  1. AgentEvent 顺序
  2. after_node_result 的执行
  3. mid-turn 注入是否属于编排契约
验证结果：
  - events: ["system_init", "text_delta", "final"]
  - node_hook: [["observe", "succeeded"]]
  - mid_turn_injection: "not an orchestrator contract"
对照关系：语义等价 + 结构性边界
结论：节点边界可观察、可扩展；进行中循环仍由具体内核拥有。
{"events": ["system_init", "text_delta", "final"], "mid_turn_injection": "not an orchestrator contract", "module": "M04", "node_hook": [["observe", "succeeded"]], "relationship": "语义等价 + 结构性边界", "status": "ok"}
IOTA_MODULE_OK M04-agent-loop-intervention

████ M05 · 会话面 ████
学习目标：读取真实 ConversationStore 与 RunStore，并对比 DSH 日志投影契约。
运行时观察：
  1. 持久化消息顺序
  2. run 状态与事件顺序
  3. 事件是否承诺 sequence 字段
验证结果：
  - messages: 2
  - run_status: "succeeded"
  - sequence_contract: "not part of the iota event schema"
对照关系：语义等价 + 结构性边界
结论：iota 保存会话和运行事实，但不声称拥有 DSH 的 seq/surface 不变量。
{"messages": 2, "module": "M05", "relationship": "语义等价 + 结构性边界", "run_status": "succeeded", "sequence_contract": "not part of the iota event schema", "status": "ok"}
IOTA_MODULE_OK M05-session-surface

████ M06 · 人在环路 ████
学习目标：验证 ACP permission 必定得到答复，并识别计划模式的宿主边界。
运行时观察：
  1. 无匹配策略时是否 fail closed
  2. permission 是否及时答复
  3. plan mode 位于哪一层
验证结果：
  - permission: "no"
  - hang_prevention: "request answered"
  - plan_mode: "kernel/harness-owned"
对照关系：语义等价 + 结构性边界
结论：权限请求由适配器可靠闭合；计划模式由具体内核或宿主管理。
{"hang_prevention": "request answered", "module": "M06", "permission": "no", "plan_mode": "kernel/harness-owned", "relationship": "语义等价 + 结构性边界", "status": "ok"}
IOTA_MODULE_OK M06-human-in-the-loop

████ M07 · 执行侧后端 ████
学习目标：确认 shell 副作用由 KernelAdapter 执行，而非编排层工具注册表。
运行时观察：
  1. kernel.shell 事件
  2. 工具结果事件
  3. 编排层 shell registry 是否为空
验证结果：
  - route: "prompt -> KernelAdapter -> kernel.shell event"
  - orchestrator_shell_registry: []
对照关系：结构性边界
结论：iota 转发执行事件，但不重写内核的 fs/shell/sandbox 执行栈。
{"module": "M07", "orchestrator_shell_registry": [], "relationship": "结构性边界", "route": "prompt -> KernelAdapter -> kernel.shell event", "status": "ok"}
IOTA_MODULE_OK M07-execution-backends

████ M08 · 委派与预设 ████
学习目标：用 GraphSpec 的依赖与绑定表达可验证的节点委派。
运行时观察：
  1. 拓扑顺序
  2. 上游输出绑定
  3. 图运行结果
验证结果：
  - order: ["research", "review"]
  - output: {"approved": "research:iota"}
对照关系：语义等价
结论：显式 DAG 依赖为委派提供确定的顺序、数据流和失败边界。
{"module": "M08", "order": ["research", "review"], "output": {"approved": "research:iota"}, "relationship": "语义等价", "status": "ok"}
IOTA_MODULE_OK M08-delegation-presets

████ M09 · 长任务与编排 ████
学习目标：观察队列租约、幂等入队与 checkpoint 恢复语义。
运行时观察：
  1. 重复 task id 是否幂等
  2. claim/ack 状态
  3. checkpoint sequence
验证结果：
  - queue: "succeeded"
  - checkpoint_sequence: 1
  - idempotent_task_id: "m09-task"
对照关系：语义等价（iota 提供更多耐久语义）
结论：队列与 checkpoint 让长任务可租约、可恢复，而不只是一段后台进程。
{"checkpoint_sequence": 1, "idempotent_task_id": "m09-task", "module": "M09", "queue": "succeeded", "relationship": "语义等价（iota 提供更多耐久语义）", "status": "ok"}
IOTA_MODULE_OK M09-long-running-orchestration

████ M10 · 外部能力接入 ████
学习目标：验证文件系统 Skill 与进程内 MCP 的真实发现和调用路径。
运行时观察：
  1. SKILL.md 同步结果
  2. MCP tools/list
  3. MCP tools/call
验证结果：
  - skill: "offline-greeter"
  - mcp: "hello iota"
对照关系：语义等价
结论：知识资产与外部工具协议都能通过明确边界接入，默认示例无需网络。
{"mcp": "hello iota", "module": "M10", "relationship": "语义等价", "skill": "offline-greeter", "status": "ok"}
IOTA_MODULE_OK M10-external-capabilities

████ M11 · 配置与数据设施 ████
学习目标：区分 profile 投影、可换存储协议与宿主持有的凭证/附件。
运行时观察：
  1. profile 文件是否真实生成
  2. 不支持的布局是否拒绝
  3. ConversationStore 是否可替换
验证结果：
  - profile: "projected"
  - storage: "memory"
  - dsh_profile_refusal: "config_profile is not supported for adapter 'dsh': it has no iota-managed config home to project into. Adapters that do: ['claude', 'hermes', 'hermes_direct']. Point the kernel at its own config file via 'command' instead (dsh: --config <cordis.yml>), and use 'env' for anything else it needs."
  - credentials_attachments: "host-owned"
对照关系：语义等价 + 结构性边界
结论：配置和会话存储可治理；凭证与附件不应被硬塞进 AgentConfig。
{"credentials_attachments": "host-owned", "dsh_profile_refusal": "config_profile is not supported for adapter 'dsh': it has no iota-managed config home to project into. Adapters that do: ['claude', 'hermes', 'hermes_direct']. Point the kernel at its own config file via 'command' instead (dsh: --config <cordis.yml>), and use 'env' for anything else it needs.", "module": "M11", "profile": "projected", "relationship": "语义等价 + 结构性边界", "status": "ok", "storage": "memory"}
IOTA_MODULE_OK M11-config-data-infrastructure

████ M12 · 框架机制本体 ████
学习目标：验证可逆 effect、身份安全 disposer 与 Provider 发现机制。
运行时观察：
  1. LIFO 与幂等 dispose
  2. 失效后注册和错名 capability 的拒绝
  3. 标准 entry-point group
验证结果：
  - lifo: ["outer", "inner"]
  - inactive_refusal: "cannot push effect 'late' onto disposed stack 'm12'"
  - capability_refusal: "BadCapability declares unknown capabilities ['typo-capability']; known capabilities: ['filesystem_skills', 'goal_commands', 'native_goal_manager', 'native_kanban_toolset']"
  - entry_points: "iota_core.providers"
  - architecture_not_imported: ["event-bus", "hmr", "intercept"]
对照关系：语义等价 + 结构性边界
结论：iota 借用可逆机制，但不复制 Cordis 的事件总线、Proxy Context 与 HMR 架构。
{"architecture_not_imported": ["event-bus", "hmr", "intercept"], "capability_refusal": "BadCapability declares unknown capabilities ['typo-capability']; known capabilities: ['filesystem_skills', 'goal_commands', 'native_goal_manager', 'native_kanban_toolset']", "entry_points": "iota_core.providers", "inactive_refusal": "cannot push effect 'late' onto disposed stack 'm12'", "lifo": ["outer", "inner"], "module": "M12", "relationship": "语义等价 + 结构性边界", "status": "ok"}
IOTA_MODULE_OK M12-framework-mechanisms
IOTA_ALL_OK modules=12 network=blocked
[exit 0]
