$ cd iota-example && env -u PYTHONHOME -u PYTHONPATH .venv/bin/python -m runtime.runner M03 --real

[claude-code:unrecognized_model] {"model":"MiniMax-M3","query_source":"sdk"}

████ M03 · 推理服务接入 ████
学习目标：理解 iota 以整个 KernelAdapter 为替换单元的设计。
运行时观察：
  1. Provider 注册的替换粒度
  2. ACP capability 声明
  3. 不支持的模型 middleware 是否编译期拒绝
真实内核证据：
  - 真实模型: MiniMax-M3
  - 真实内核: claude（claude-agent-sdk + claude）
  - 真实响应: 整个 KernelAdapter 作为一个原子单元整体替换，保证内核状态一致性。
  - 事件序列: step_start, system_init, thinking, text_delta, final, step_end
三类边界：
  - MiniMax-backed kernel 行为: 换成 MiniMax 只换了内核这一个替换单元，编排层代码一行未改。
  - iota 编排层本地机制断言 : Provider 注册粒度、ACP capability 声明与不支持项的拒绝在本地断言。
  - 明确不属于 iota 的能力  : 单次模型调用的 middleware 属于内核，跨进程内核不声明未验证过的能力。
验证结果：
  - replaceable_unit: "KernelAdapter"
  - acp_refusal: "node 'infer': kernel 'acp' cannot activate per-node model middleware/hooks ['llm_execution']; remove them or use a kernel that advertises per_session_middleware"
对照关系：结构性边界
结论：iota 替换整个内核适配器，不在编排层仿造 DSH 的 LLM middleware。
{"acp_refusal": "node 'infer': kernel 'acp' cannot activate per-node model middleware/hooks ['llm_execution']; remove them or use a kernel that advertises per_session_middleware", "module": "M03", "relationship": "结构性边界", "replaceable_unit": "KernelAdapter", "status": "ok"}
IOTA_REAL_MODULE_OK M03-inference-service-access kernel=claude model=MiniMax-M3 chars=40 events=6
[exit 0]
