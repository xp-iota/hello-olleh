# 真实 @deepseek-ai/* 包里有若干 peer 依赖尚未发布到公共 registry
# （dsh-bash-env / dsh-type-meta / dsh-code-runtime-worker）。
# npm 默认会尝试自动安装 peer → 404。legacy-peer-deps 让 npm 跳过 peer 自动安装，
# 直接依赖下面 dependencies 里显式列出的那一套（版本线已对齐到 0.1.5-rc.2）。
legacy-peer-deps=true
