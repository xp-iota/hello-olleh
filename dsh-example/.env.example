# MiniMax（稀宇科技）provider 配置模板 —— 复制为 .env 并填入真实密钥。
# 本模板可入库（.gitignore 的 !.env.example 例外）；.env 本身永不入库。

# MiniMax 平台密钥：https://platform.minimaxi.com/user-center/basic-information/interface-key
MINIMAX_API_KEY=

# Anthropic 兼容端点（国内区）与模型 id —— 不设则用适配器内置默认值。
# MINIMAX_BASE_URL=https://api.minimaxi.com/anthropic
# MINIMAX_MODEL=MiniMax-M3

# runTurn 默认 provider 选路（mock / minimax-m3）。
# 注意：设为 minimax-m3 后 `npm run all` 会对每个 runTurn 示例发起真实请求，套件不再离线。
# DSH_PROVIDER=minimax-m3
