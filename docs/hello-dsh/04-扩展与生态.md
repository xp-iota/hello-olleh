---
layout: content
title: "04 扩展与生态：Skill、插件、协议、SDK"
parent_url: /docs/hello-deepseek-harness/
---

# 04 扩展与生态：Skill、插件、协议、SDK

> **本篇回答**：怎么给 DSH 加能力而不改它。四条路径按"要不要写代码"排开：**Skill**（纯 Markdown 数据包，模型自己发现）、**hooks 桥接**（兼容 Claude Code / Codex 的钩子配置）、**Extensions**（让 cordis 插件树本身成为可操作对象，含让模型改自己运行时的工具）、**协议与 SDK**（MCP / ACP 两个方向相反的协议，加把 dsh 当子进程驱动的 Python SDK）。
> **路径缩写**：`P/` = `packages/`，`PY/` = `python/`。行号对应快照 `47f94385`。
>
> 🧭 **本篇导览**：4.1–4.10 扩展机制与插件生态 → 4.11–4.17 MCP / ACP / Host / API → 4.18–4.26 Python SDK 与多语言运行时。
>
> 📎 **来源**：本篇合并自原 16 / 17 / 23 篇，章节已重新连续编号为 4.x。

## 4.1 两条扩展路径的区别

> 📐 **配套可跑示例**：[`08-skill-code-review`](../../dsh-example/08-skill-code-review/SKILL.md) 是"按数据扩展"的完整样本
> （一个 `SKILL.md`，零行 TypeScript）；它的 [`run.ts`](../../dsh-example/08-skill-code-review/run.ts) 把这份 Markdown
> 注册进真实 `ctx.skills`，再用真实 `renderSkillContent` 渲染后经 `agent.inject()` 送进下一步上下文。


![两条扩展路径：插件与 Skill](diagrams/04-two-extension-paths.svg)

**两条扩展路径：插件与 Skill** — [交互版](diagrams/04-two-extension-paths.html)（明暗主题 / 缩放 / 关系追踪 / 导出） · [IR 源](diagrams/04-two-extension-paths.architecture.json)

- **怎么选**：要新增一种能力接缝就写插件 · 只是给模型加一份可发现的操作说明就用 Skill · Skill 改完立即生效，插件需要重启
- **Skill 的三个组成**：skill 定义能力与渲染规则 · skill-filesystem 从文件系统发现 skill · tool-skill 让模型能列出并调用
- **类比**：Skill 相当于 Claude Code 的 skills 与 slash commands · 插件相当于扩展宿主本身 · 前者是内容，后者是机制

## 4.2 `skill` 组的 4 个包

| 包 | src 行数 | 角色 |
|---|---|---|
| `skill-filesystem` | 1071 | Provider（**从文件系统加载 skill**） |
| **`skill`** | **898** | Service Definition（`ctx.skills`） |
| `tool-skill` | 461 | Consumer（模型调用 skill 的工具） |
| `skill-badge` | 90 | UI 徽章（前端有 `ui-skill`） |

💡 **`skill-filesystem` 是唯一的 provider**（1071 行），说明 skill 目前只从文件系统来。但 seam 结构在那里——将来可以有 `skill-remote`、`skill-registry` 等。

💡 **`skill-badge` 只有 90 行**：给 UI 提供"这个 skill 的标识/图标"信息。配合 `client/ui-skill` 子包。

## 4.3 `hooks` 组：兼容其它 agent 的钩子协议

> 📐 **配套可跑示例**：[`09-hooks-lifecycle-steering`](../../dsh-example/09-hooks-lifecycle-steering/index.ts) 演示这些桥接器最终落到的那组拦截点
> （`agent/session-start` · `pre-step` · `request` · `turn-stopping`），并在轮末用 `agent.steer()` 真的多跑了一个 step。


`packages/hooks/`（1813 行 src）：

| 包 | src 行数 | 作用 |
|---|---|---|
| `hook-protocol` | 854 | 钩子协议定义 |
| **`hooks-claude-code`** | 514 | **兼容 Claude Code 的 hooks** |
| **`hooks-codex`** | 445 | **兼容 Codex 的 hooks** |

💡 **这是与 [08 § 8.27](08-执行侧服务-文件Shell沙箱子代理压缩.md) 呼应的另一处"兼容竞品"**：
- `subagent-claude-code` / `subagent-codex`：把竞品当**执行后端**
- `hooks-claude-code` / `hooks-codex`：让**为竞品写的 hooks 在 DSH 里能跑**

💡 **迁移成本策略**：用户已有的 Claude Code hooks 配置不需要重写。这是很务实的生态策略——降低切换门槛。

## 4.4 `extensions` 组：cordis 树本身成为对象

`packages/extensions/`（16090 行，含 tsx）：

| 包 | 行数 | 作用 |
|---|---|---|
| **`tool-cordis`** | **5984** | **让模型操作 cordis 插件树的工具** |
| `cordis-client-runner` | 5080 | 在 client 侧跑 cordis 树 |
| `cordis-host-runner` | 3360 | 在 host 侧跑 cordis 树 |
| `ui-cordis` | 1666 | 前端的插件树查看/编辑器 |

![让模型和人都能操作插件树](diagrams/04-cordis-self-hosting.svg)

**让模型和人都能操作插件树** — [交互版](diagrams/04-cordis-self-hosting.html)（明暗主题 / 缩放 / 关系追踪 / 导出） · [IR 源](diagrams/04-cordis-self-hosting.architecture.json)

- **模型与人共用一套底座**：tool-cordis 是模型侧入口 · ui-cordis 是人类侧入口 · 两者都读写同一棵插件树
- **两个 runner 的差异**：host runner 跑在 Node 进程里 · client runner 跑在浏览器里 · 后者要绕开模块加载与文件系统限制，因此代码更多
- **这是自举能力**：模型可以增删自己的能力配置行 · 也能查询服务是否就绪、依赖是否满足 · 相当于把 cordis 的运维面开放给了 agent

💡 **为什么 client-runner 比 host-runner 大**：浏览器里没有文件系统、没有 `node:module` 的内部 loader（cordis 的 loader 依赖它，[cordis 08 § 8.4](../hello-cordis/08-HMR热重载.md)），所以要另做一套模块解析与加载。

## 4.5 插件市场：`plugin-inventory`

`P/host/plugin-inventory`（在 host 组）+ `client/ui-settings-plugin-inventory`。

💡 **`dsh plugin --profile x add <package>`** 是 `loadProfile` 抛错时提示的命令（[05 § 5.4](05-启动与Cordis落地.md)）。配合 profile 目录本身就是 pnpm workspace（`PROFILE_PNPM_WORKSPACE`，`AB/profile.ts:138`），**用户能直接 `pnpm add` 第三方插件包**。

💡 **`dsh-plugin` 是 GitHub topic**（README 提到）：

> Add the [`dsh-plugin`](https://github.com/topics/dsh-plugin) topic to your plugin repository for discoverability.

**插件发现靠 GitHub topic + npm**，没有中心化市场。

## 4.6 `preset` 组：能力集打包

`packages/preset/`（3794 行）：`agent-presets` + `persona`。

💡 **agent preset 是"给一个会话不同能力集"的载体**（[03 § 3.10](03-能力缝与服务全景.md)）。它是一段 cordis 配置（含 group 行 + `isolate` 声明）。

💡 **`persona` 单独一个包**对应 `PERSONA_SECTION`（[07 § 7.20](07-请求管线-LLM工具与提示.md)）——人格可以打包分发。

## 4.7 `feedback` 与 `identity`

| 组 | 包 | 作用 |
|---|---|---|
| `feedback`（2155 行） | `command-feedback`、`message-feedback` | 用户反馈（前端 `ui-message-feedback`） |
| `identity`（248 行） | `anonymous-user-id` | **匿名用户 id** |

💡 **`anonymous-user-id` 只有 248 行**，且组名叫 `identity`。这是遥测/反馈需要的最小身份标识——**刻意做成匿名的**。

## 4.8 `runtime-diagnostics`

`P/runtime-diagnostics/invariants`（540 行）。

💡 **这是不变量框架本身**——全仓 219 个 `invariant.ts`（[02 § 2.5](02-代码结构地图.md)）都建在它上面。540 行提供断言、错误格式化、开关控制等基础设施。

## 4.9 `util` 组：7 个小工具包

`packages/util/`（2204 行）：

| 包 | 作用 |
|---|---|
| `atomic-write` | 原子写文件（对照 [cordis 07 § 7.5](../hello-cordis/07-Loader与配置树.md) 的 `.tmp` + rename） |
| `brand` | 品牌类型（nominal typing） |
| `home-paths` | Harness home 路径解析 |
| `launch-environment` | 启动环境 |
| `native-command` | 原生命令 |
| `output-retention` | 输出保留策略 |
| `timeout` | 超时工具 |

💡 **`atomic-write` 与 `output-retention` 单独成包**说明这两件事在多处被需要：前者用于所有配置/状态落盘，后者用于工具输出的截断策略（与 `spill`、`compaction-tool-result-pruner` 相关）。

## 4.10 扩展点全景

> 📐 **配套可跑示例**：这张全景表里的每个扩展点都有对应示例，逐条映射见
> [示例工程 README 的能力索引](../../dsh-example/README.md)（26 个目录，全部跑在真实 `@deepseek-ai/*` 发布包上）。


把前面各篇的扩展点汇总（对应 `docs/architecture.md` 的映射表 + 本文档集的发现）：

![按事件扩展的六组扩展点](diagrams/04-extension-events.svg)

**按事件扩展的六组扩展点** — [交互版](diagrams/04-extension-events.html)（明暗主题 / 缩放 / 关系追踪 / 导出） · [IR 源](diagrams/04-extension-events.architecture.json)

- **emit 与 waterfall 的区别**：emit 型只能观测，不能改结果 · waterfall 型可以改写参数、替换返回值或短路 · 扩展前先确认目标事件是哪一种
- **最常用的三个切入点**：tools 的三段 waterfall 用于审批与改写工具调用 · llm/stream 用于替换或包裹模型调用 · system-prompt/assemble 用于注入上下文
- **对应上游文档**：这份清单对应 docs/architecture.md 的扩展点映射 · 每组事件的完整签名在各章节展开 · 策略包只依赖事件，不依赖实现

---

> 📎 **以下承接原「17 MCP 与 ACP 协议集成」。**
> 本篇讲 DSH 对两个外部协议的接入：**MCP**（Model Context Protocol，作为客户端消费外部工具）与 **ACP**（作为服务端暴露 agent 能力）。方向相反，值得对照看。
> 路径缩写：`P/` = `packages/`。行号对应快照 `47f94385`。

## 4.11 两个协议的方向相反

![MCP 与 ACP 的方向相反](diagrams/04-mcp-vs-acp.svg)

**MCP 与 ACP 的方向相反** — [交互版](diagrams/04-mcp-vs-acp.html)（明暗主题 / 缩放 / 关系追踪 / 导出） · [IR 源](diagrams/04-mcp-vs-acp.architecture.json)

- **两个协议角色相反**：MCP 里 DSH 是客户端，去消费别人的工具 · ACP 里 DSH 是服务端，被别人驱动 · 因此两者可以同时启用而不冲突
- **MCP 的落点是 ctx.tools**：外部工具被注册成普通工具 · 模型看不出它来自远端 · 权限与审批仍走本地的 tools waterfall
- **ACP 只面向自动化**：注释里明确写了 automation-only · 人机交互界面由 host/webserver 负责 · 两者分工清晰

## 4.12 `mcp-client`（929 行）

`P/mcp/mcp-client/src/index.ts`：

| 符号 | 行号 | 说明 |
|---|---|---|
| `export const name = 'mcp-client'` | 28 | 插件名 |
| `'mcp-client.serverName'` effect label | 161 | |
| `'mcp-client.connection'` effect label | 170 | |

💡 **两个 effect label 揭示了结构**：一个 effect 管"服务器名字"的注册，另一个管"连接"。都是 cordis effect（[cordis 03 § 3.2](../hello-cordis/03-Fiber模型.md)），所以**卸载插件即断开连接、摘掉工具**。

💡 **`mcp` 组只有一个包**（不像其它组有 definition + provider）。因为 MCP 客户端不是 seam —— 它是 `ctx.tools` 的一个**消费方**（把外部工具搬进来），本身不定义新能力。

💡 **929 行的量级**说明它处理了 MCP 的协议细节：传输（stdio/SSE）、握手、工具列表同步、schema 转换、错误映射。

## 4.13 `acp`（532 行）：automation-only 服务端

`P/acp/acp/src/index.ts`。这个包的注释信息密度很高。

| 符号 / 注释 | 行号 | 内容 |
|---|---|---|
| `@module @deepseek-ai/dsh-acp` | 9 | |
| 从 `./codec.ts` 导入 | 40 | `acpPromptToText`、`promptHasUnsupportedContent`、`turnEndToStopReason` |
| `export const name = 'acp'` | 42 | |
| 插件 config 注释 | 69 | "the provider/model selection used for each ACP-created agent" |
| **"Mount the automation-only ACP server"** | **101** | |
| 注入作用域注释 | 106 | "ACP handlers execute **outside this plugin's injection scope**, so capture the ..." |
| disposed 检查 | 121 | `if (closed) throw internalError('the ACP bridge has been disposed')` |
| `session/update` 失败告警 | 134 | `logger.warn('acp: session/update failed: ...')` |
| **权限请求注释** | **212-213** | 见下 |

### 三个值得注意的设计

![ACP 实现里三个值得注意的设计](diagrams/04-acp-designs.svg)

**ACP 实现里三个值得注意的设计** — [交互版](diagrams/04-acp-designs.html)（明暗主题 / 缩放 / 关系追踪 / 导出） · [IR 源](diagrams/04-acp-designs.architecture.json)

- **第二条是最容易踩的坑**：handler 执行时 ctx 的注入链已经不同 · 在 handler 里现取服务会拿到错误的视图 · 必须在插件激活期就把引用捕获进闭包
- **为什么会这样**：cordis 的 traceable 让 ctx 属性访问依赖调用方 · ACP handler 由外部请求触发，不在插件调用栈上 · 于是访问链断了
- **权限通道的定位**：它不是给人看的确认弹窗 · 而是给上游 agent 的机器可读策略接口 · 因此响应格式是结构化的，不含展示文案

### `codec.ts` 的三个转换函数

| 函数 | 方向 | 作用 |
|---|---|---|
| `acpPromptToText` | ACP → DSH | 把 ACP 的 prompt 结构转成文本 |
| `promptHasUnsupportedContent` | 校验 | 检测 ACP prompt 里有 DSH 不支持的内容 |
| `turnEndToStopReason` | DSH → ACP | 把 `TurnEndReason`（[06 § 6.4](06-Agent循环与会话日志.md)）映射成 ACP 的 stop reason |

💡 **`promptHasUnsupportedContent` 的存在是诚实的**：协议桥接必然有能力差异，明确检测并报错比静默丢内容好。

💡 **`turnEndToStopReason` 把 DSH 的六种 turn 结束原因**（`completed` / `max-tokens` / `blocked` / `aborted` / `error`）映射到 ACP 的词汇表。

## 4.14 `acp-snapshot`：协议的快照测试

`P/test-support/acp-snapshot` + `vitest.snapshot.config.ts` 的完整机制（录制一次、无密钥回放）见 [10 § 10.4](10-测试与工程实践.md)。

💡 **给协议桥接做快照测试是正确做法**：协议交互的报文格式必须稳定，快照能捕获无意的变更。

💡 **`examples/acp-demo`** 是可运行的 ACP 示例，`package.json` 里有脚本：

```
demo:acp = node --import tsx packages/examples/acp-demo/src/bin.ts --config examples/acp-agent/cordis.yml
```

另有 `examples/jsonrpc-demo`（ACP 基于 JSON-RPC）。

## 4.15 `host` 组：8 个包

`packages/host/`（22412 行含 tsx）：

| 包 | 行数 | 作用 |
|---|---|---|
| **`apiproxy`** | **8571** | **API 代理**（全组最大） |
| `directory-picker-native` | 737 | 原生目录选择器 |
| `directory-picker-browse` | 349 | 浏览式目录选择器 |
| **`webserver`** | **325** | **HTTP/WS 服务器** |
| `directory-picker-auto` | 220 | 自动选择实现 |
| `directory-picker` | 168 | Definition |
| `frontend-static` | 144 | 静态资源服务 |
| `plugin-inventory` | 120 | 插件清单 |

💡 **`webserver` 只有 325 行**，而 `apiproxy` 有 8571 行（26 倍）。说明"起个 HTTP 服务"很简单，**难的是把 DSH 的能力安全地代理出去**——鉴权、请求映射、流式转发、错误处理。

💡 **`directory-picker` 是一个四包 seam**：definition（168）+ 三个 provider（native 737 / browse 349 / auto 220）。前端对应 `ui-directory-picker-native` 与 `ui-directory-picker-browse`。**选目录这件小事也做成了可替换的**——因为 Electron/浏览器/终端环境的实现完全不同。

## 4.16 `api` 组：网关

`packages/api/`（1807 行 src）：

| 包 | src 行数 | 作用 |
|---|---|---|
| `gateway` | 1358 | API 网关（`dsh-base` patch 里 id 为 `typert-gateway`） |
| `remotes` | 449 | 远端连接 |

💡 **`dsh-base` 的 patch 里 `typert-gateway` 用的是 `@deepseek-ai/dsh-api-gateway`**（[05 § 5.11](05-启动与Cordis落地.md)）。id 叫 `typert-gateway` 说明它与 TypeRT（[09 篇](09-宿主与运行面-Web网关编排存储类型.md)）紧密相关——**网关的接口是由 TypeRT 的类型注册表驱动的**。

## 4.17 三个协议接口的对照

| 维度 | MCP | ACP | DSH SDK |
|---|---|---|---|
| DSH 的角色 | **客户端**（消费） | **服务端**（暴露） | **两端都有** |
| 包 | `mcp-client`（929） | `acp`（532） | `sdk/{client,protocol,server}`（4471） |
| 用途 | 引入外部工具 | 被自动化驱动 | DSH↔DSH 通信 |
| 对应 subagent provider | — | `subagent-acp`（587） | `subagent-dsh-sdk`（375） |
| 示例 | — | `examples/acp-demo` | — |
| 测试支持 | — | `test-support/acp-snapshot` | — |

💡 **`sdk` 组有 client / protocol / server 三个包**（[04 篇](04-扩展与生态.md)），是 DSH 自己的协议。Python SDK（`python/sdk`）也基于它。

---

> 📎 **以下承接原「23 Python 与多语言 SDK」。**
> 本篇讲 DSH 怎么被别的语言驱动：`sdk` 组三包（TS 侧协议）、Python SDK 两包（把 dsh 当子进程 + JSON-RPC over stdio）、以及单文件 exe 打包。
> 路径缩写：`P/` = `packages/`，`PY/` = `python/`。行号对应快照 `47f94385`。

## 4.18 两层结构

![SDK 的两层结构](diagrams/04-sdk-two-layers.svg)

**SDK 的两层结构** — [交互版](diagrams/04-sdk-two-layers.html)（明暗主题 / 缩放 / 关系追踪 / 导出） · [IR 源](diagrams/04-sdk-two-layers.architecture.json)

- **protocol 是唯一的共享契约**：client 与 server 都只依赖它 · Python 侧按同一协议重新实现 · 于是跨语言不需要代码生成
- **Python 分成两个发行包**：sdk 是纯 Python 客户端 · sdk-runtime 装的是预编译的 DSH 可执行文件 · 两者版本对应，缺一不可
- **高低两层 API**：高层是 turns API，面向常见用法 · 低层是 JSON-RPC 客户端，可完全控制 · 需要自定义流程时降到低层

💡 **选 stdio JSON-RPC 而非 HTTP 的理由**：把 dsh 当子进程驱动时，stdio 天然有生命周期绑定（父进程退出子进程也退）、无需端口分配、无需鉴权。与 MCP 的 stdio 传输、LSP 的 stdio 传输是同一套思路。

## 4.19 Python SDK 的 5 个模块

`PY/sdk/src/deepseek_harness/`（873 行）：

| 文件 | 行数 | 职责 |
|---|---|---|
| **`client.py`** | **557** | 低层 JSON-RPC 客户端（占 64%） |
| **`api.py`** | **242** | 高层 turns API |
| `models.py` | 32 | 数据模型 |
| `errors.py` | 23 | 错误类型 |
| `__init__.py` | 19 | 导出 |

`PY/sdk-runtime/src/deepseek_harness_runtime/__init__.py`：164 行。

💡 **两层 API 设计**（README 明确说 "High-level turns API and lower-level JSON-RPC client"）：
- `api.py`（242 行）：面向"跑一个 turn"的高层用法
- `client.py`（557 行）：面向"精确控制协议交互"的低层用法

💡 **`models.py` 只有 32 行**——Python 侧的数据模型很薄，因为大部分结构直接用 dict（JSON-RPC 传过来的）。

## 4.20 运行时选择契约

`PY/README.md` 的关键段：

> The SDK **starts the matching bundled runtime** unless the caller selects an explicit channel. The client **selects the channel** and supplies default configuration; **the runtime itself always requires an explicit configuration**.

![SDK 的 channel 与配置契约](diagrams/04-sdk-channel-contract.svg)

**SDK 的 channel 与配置契约** — [交互版](diagrams/04-sdk-channel-contract.html)（明暗主题 / 缩放 / 关系追踪 / 导出） · [IR 源](diagrams/04-sdk-channel-contract.architecture.json)

- **与 boot() 的契约一致**：DSH 启动必须有 config path，见 05 篇 § 5.5 · Python SDK 没有例外 · 差别只在谁来提供这份配置
- **这是 fail-closed 的延伸**：runtime 拒绝在缺配置时启动 · 便利层只允许加默认值，不允许跳过校验 · 同一原则见 08 篇 § 8.23
- **channel 的两种来源**：调用方显式给出，用于连到已有实例 · 不给则由 SDK 启动匹配版本的 bundled runtime · 版本匹配由 wheel 依赖保证

## 4.21 单文件 exe：`sdk-runtime` 的打包

`pnpm-workspace.yaml` 把 `python/sdk-runtime` 列为 workspace 成员，注释说明用途：

> Deploy root of the single-exe build: a pure dependency manifest whose closure is what the exe bundles and what the Python runtime distributes.

![用空包的依赖闭包定义打包范围](diagrams/04-exe-packaging.svg)

**用空包的依赖闭包定义打包范围** — [交互版](diagrams/04-exe-packaging.html)（明暗主题 / 缩放 / 关系追踪 / 导出） · [IR 源](diagrams/04-exe-packaging.architecture.json)

- **这个做法的好处**：不需要手工维护打包白名单 · 加依赖即自动进入打包范围 · 漏掉依赖会在构建期暴露而不是运行期
- **配套脚本**：scripts/build-python-release.py 构建 wheel · scripts/check-macos-deployment-target.py 校验最低系统版本 · PY/sdk-runtime/hatch_build.py 是 hatch 构建钩子
- **为什么用空包**：空包只表达依赖关系，不引入额外代码 · 它的闭包就是运行时的最小充分集 · 语义清晰且可被工具直接计算

## 4.22 Python 侧的 7 个测试

`PY/sdk/tests/`：

| 文件 | 作用 |
|---|---|
| `test_client.py` | 客户端 |
| `test_smoke_model.py` | 冒烟（模型调用） |
| `test_runtime_resolution.py` | **运行时解析**（§ 4.20 的契约） |
| `test_bundled_runtime.py` | **打包的运行时** |
| `test_release_version.py` | **版本一致性** |
| `test_macos_deployment_target.py` | macOS 部署目标 |
| `manual_sdk_agent_smoke.py` | **手动**冒烟（文件名带 `manual_`，不自动跑） |

根目录有 `pytest.ini`（[02 § 2.9](02-代码结构地图.md)）。

💡 **`test_release_version.py` 对应 `.gitlab-ci.yml` 的校验**（[02 § 2.9](02-代码结构地图.md)）：

```
test "$CI_COMMIT_TAG" = "python-v$DSH_VERSION" || { echo "Tag ... does not match package.json version ..."; exit 1; }
```

**tag 必须等于 `package.json` 的 version** —— 双重校验（CI 脚本 + Python 测试）。

💡 **`manual_` 前缀是一种约定**：需要真实模型调用/网络的测试不自动跑，避免 CI 依赖外部服务。

## 4.23 `subagent-dsh-sdk`：SDK 的自用

`P/subagent/subagent-dsh-sdk`（375 行，[08 § 8.27](08-执行侧服务-文件Shell沙箱子代理压缩.md)）。

💡 **DSH 用自己的 SDK 把另一个 dsh 实例当作 subagent**。这是"协议自洽"的检验：如果 SDK 好用，自己就会用它。

## 4.24 三种对外协议的最终对照

| 维度 | MCP | ACP | **DSH SDK** |
|---|---|---|---|
| DSH 角色 | 客户端 | 服务端 | **两端** |
| 传输 | stdio / SSE | JSON-RPC | **行分隔 JSON-RPC over stdio** |
| TS 包 | `mcp-client`（929） | `acp`（532） | `sdk/{client,protocol,server}`（1754） |
| 其它语言 | — | — | **Python（2629 行）** |
| 对应 subagent provider | — | `subagent-acp`（587） | `subagent-dsh-sdk`（375） |
| 示例 | — | `examples/acp-demo`、`jsonrpc-demo` | — |
| 发布物 | — | — | 2 个 PyPI 包 + 单文件 exe |
| CI | — | `acp-snapshot` 快照测试 | `python-release.yml` + GitLab CI |

💡 **只有 DSH SDK 有多语言实现**。MCP 与 ACP 是"接别人的协议"，SDK 是"定自己的协议"，所以只有后者需要跨语言客户端。

## 4.25 `native/landlock-run` 的对照

另一个"独立发布的多平台组件"（[08 § 8.19](08-执行侧服务-文件Shell沙箱子代理压缩.md)）：

| 维度 | `landlock-run` | `sdk-runtime` |
|---|---|---|
| 语言 | C11（~300 行） | Node（打包成 exe） |
| 发布 | npm（入口包 + 平台包） | PyPI（wheel 带二进制） |
| 平台选择 | npm 的 `os`/`cpu` 字段 | wheel 的 platform tag |
| 无回退策略 | **无安装期编译** → probe 报 unusable → fail closed | — |
| CI | `landlock-run.yml` + `-release.yml` | `build-exe-for-python-sdk.yml` + `python-release.yml` |

💡 **两者都采用"预编译多平台二进制 + 薄入口包"模式**，这是 Node/Python 生态分发原生组件的标准做法。

## 4.26 `examples/jsonrpc-demo`

`packages/examples/jsonrpc-demo`（在 examples 组，2858 行含三个示例）。

💡 **ACP 基于 JSON-RPC**，SDK 也用 JSON-RPC。这个 demo 大概演示裸 JSON-RPC 交互，作为两者的基础参考。

---

**上一篇** ← [03 能力缝 Seam 与服务全景](03-能力缝与服务全景.md) ｜ **下一篇** → [05 启动流程与 Cordis 落地](05-启动与Cordis落地.md)：从 `dsh web` 到插件树就绪的完整链路，以及底下那份 vendored cordis。
